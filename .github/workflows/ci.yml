name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# A newer push to the same branch/PR cancels the older run still in flight.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        # The test scripts use `node --experimental-strip-types`, which needs Node >= 22.6.
        # 20 cannot run the suite even though package.json engines says >=20 — see README/notes.
        node: [22, 24]
    name: Node ${{ matrix.node }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - name: Install
        run: npm ci

      # Fast, clear failures first: types, then a real build, then tests by widening scope.
      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build

      - name: Test (simple)
        run: npm run test:simple

      - name: Test (full)
        run: npm run test:full

      - name: Test (edge)
        run: npm run test:edge
